_2026-07-29_

## English

kmod: getsockname v6 handed out the wrong iface's fake — first prefix-rule match can't tell ifaces apart inside the shared carrier /32; learn a real→fake /64 map on the netlink path and compose with the iface-true fake

## Русский

kmod: getsockname v6 выдавал фейк не того интерфейса — совпадение по первому prefix-правилу не различает интерфейсы в общем carrier /32; учим карту real→fake /64 на netlink-пути и собираем с правильным фейком интерфейса
