_2026-07-29_

## English

/proc/net/route Destination/Gateway columns and single-lookup route queries (ip route get) rewrite covered IPv4 addresses to the fake as well — netlink dumps (fib_dump_info), netlink single queries (new rtnl_unicast hook), and procfs now present one identical routing view

## Русский

Колонки Destination/Gateway в /proc/net/route и одиночные запросы маршрутов (ip route get) теперь тоже подменяют покрытые IPv4-адреса фейком — выгрузки netlink (fib_dump_info), одиночные запросы netlink (новый хук rtnl_unicast) и procfs показывают одну и ту же таблицу маршрутов
