_2026-07-29_

## English

kmod: getname retval gate expects 0 but success returns the sockaddr length (16/28) since 4.17 — gate on >= 0 so the getsockname rewrite actually runs

## Русский

kmod: проверка retval в getname ждала 0, но успех возвращает длину sockaddr (16/28) начиная с 4.17 — проверяем >= 0, чтобы rewrite getsockname реально работал
